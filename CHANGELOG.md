# 更新日志

本文件同时是程序内「检查更新」页面的数据源：升级检测到新版本时，
会把这里对应版本的条目直接显示给用户看，所以每条都要写清楚
**改了什么、为什么改、对用户有什么影响**，不要只写"修复若干问题"。

格式说明：版本号遵循 `主版本.次版本.修订号`。
- ⚠️ 标记的条目表示**会改变实盘行为**，升级前请务必读完。
- 🔒 标记的条目和资金安全直接相关。

---

## 1.0.0 — 2026-08-22

首个正式 GitHub Release。这个版本把已经完成的交易安全修复、真实历史资金费率、
组合级回测、图形化操作和在线更新能力整理为一个可下载、可追踪的正式基线。

### 发布范围

- **建立正式版本基线。** 版本号、更新日志、Git 标签和 GitHub Release 从本版开始统一使用 `v1.0.0`。
- **校正文档与当前实现。** README 中的信号权重、Regime 衰减、杠杆上限、调仓缓冲区、
  历史资金费率和已移除的反手规则均已与代码及示例配置对齐。
- **加固发布流程。** 发布脚本现在会拒绝意外文件改动和非 `main` 分支，远端检查失败时中止，
  支持只检查不发布，并用原子推送一起发布分支与标签。

### 升级影响与边界

- 从 `0.9.0` 升级到本版**没有新增策略行为变更**；交易逻辑与风险参数仍以 `0.9.0` 条目所列内容为准。
- 本版的“正式”表示版本、文档和交付流程进入稳定基线，**不表示策略已经证明具有稳定盈利能力**。
  当前显著性检验仍不足以支持直接实盘使用；默认保持 `PAPER`，请先回测并观察模拟盘。

## 0.9.0 — 2026-08-22

首个打包发布版本。

### 🔒 资金安全相关修复

- ⚠️ **保证金模式改为全仓，并在每次开仓前回读校验。**
  之前一直开成逐仓——根因是 Gate 用 `leverage` 字段本身编码保证金模式
  （`"0"` 才是全仓，非零值是逐仓倍数），而代码传的是具体倍数。
  现在改为传 `"0"` + `cross_leverage_limit`，并且设置后立即回读确认：
  若该合约已有逐仓持仓导致切换被拒，会**拒绝开新仓**并提示先去官网平仓，
  但不会阻塞减仓/平仓（降低风险的动作任何时候都放行）。
- ⚠️ **修复平仓请求被交易所拒绝的 bug。** `close_dual` 传的 `auto_size`
  用了 `dual_long`/`dual_short`，而 Gate 要求的是 `close_long`/`close_short`
  （前者是持仓 mode 字段的值，不是 auto_size 的合法值）。
- **持仓同步失败后不再继续交易。** 之前同步失败只写日志就继续跑，
  可能把已有实仓误判成空仓而重复开仓；现在会整轮跳过。
- **下单改为按实际成交量记账。** 之前假设 IOC 单全部成交，
  现在读取返回的 `left` 字段，部分成交会如实记录。
- **反手改为原子操作。** 任一动作下单失败会中止该标的本轮剩余动作，
  避免"平仓失败后继续开反向仓"导致同时留下多空两条腿。
- **移除交易标的后的遗留持仓会被自动清仓**，不再无人管理。

### ⚠️ 策略行为变更

- **调仓缓冲区默认值 6% → 25%。** 这个值是相对"满预测仓位"算的，
  6% 只相当于 forecast 抖动 0.6 个单位就触发调仓，而 forecast 每根 1H K线
  都会重算，导致海量零信息交易。实测（90天双标的）：6% 时 116 笔、
  年化换手 61 倍、毛利 618；25% 时 29 笔、换手 38 倍、毛利 951。
  注意毛利也变好了——频繁再平衡不只是费钱，它在系统性砍掉盈利仓位。
- **手续费率改为以你填写的为准。** 之前交易所返回的公开基础费率优先，
  设置页填的只当兜底值，导致回测成本被系统性高估（实测隐含 0.069% vs 实际 0.05%）。
- **滑点默认 5bp → 2bp。** 这项成本不计入"手续费"而是体现在成交价里，
  很容易被忽略；5bp 对 BTC/ETH 几千 U 的市价单过于悲观。
- **删除两条经消融实验证明无效的手工规则**：最短持仓时间（−0.42pp）
  和反转状态机（−5.15pp）。相关的 5 个配置项一并移除。

### 数据与回测正确性

- **接入真实历史资金费率。** Gate 是提供该接口的（此前误判为不提供），
  回测的 carry 信号与资金费成本改用逐期真实费率，不再拿单一快照贴一整段历史。
- **修复 K 线缓存丢弃新数据的 bug。** 合并缓存时 `drop_duplicates` 保留了旧记录，
  导致未收盘 K 线的旧快照被永久锁死（曾出现 4H 成交量只有 66,061 而
  同时段 1H 合计 13,010,952 的异常）。
- **信号计算剔除未收盘 K 线。** 之前会用当前还在变动的那根 K 线算信号，
  造成同一根 K 线内反复开平仓，且与回测口径不一致。
- **修复模拟盘完全无法运行的 bug。** `PaperExchange` 缺少 `.api`/`.settle`
  转发，导致模拟盘每轮都在算信号这一步崩掉、永远开不出仓位。

### 新增功能

- **K 线图页面**：蜡烛图上标注买卖点，实盘/模拟盘与回测两个视图。
- **手动开单测试页**：按名义价值下单、独立设置杠杆与止盈止损，
  贵金属与加密分两个面板，逐步显示每一步 API 调用结果，便于排查连通性。
- **滚动样本外验证**：把回测窗口切成多折分别统计，看表现是否只靠某段行情撑起。
- **趋势捕获诊断**：自动识别显著趋势段并逐段打分（参与率／捕获率／
  入场滞后／离场过早），用于分辨"漏趋势"还是"抓到了但拿不满"。
- **成本分解面板**：直接给出未扣成本毛利／手续费／滑点／资金费，
  以及平均每笔成交额和杠杆，不用再靠反推。
- **启动脚本容错**：Windows 上检测不到 Python 时可选自动安装；
  依赖装到一半失败下次会自动重试（此前会永久跳过安装、卡在 ImportError）。

### 已知限制

- 显著性检验显示，本策略在 BTC/ETH 的 1H/4H 尺度上**测不出统计显著的边际**
  （置换检验 p = 0.165~0.93）。周期越慢表现越好（4H/1D 档 Sharpe 0.87
  vs 1H/4H 档 −0.09），但样本量不足以下定论。**不建议直接用于实盘盈利**，
  当前更适合作为研究框架使用。详见 README「滚动样本外验证与趋势捕获诊断」。
- 订单簿深度（OBI）类信号因 Gate 只提供实时快照、无历史，无法回测验证，
  故未纳入决策链路。
