# ⚠️ 当前版本已经不再从 .env 读取密钥了，这个文件仅作历史保留。
# API Key/Secret 请在网页「设置 → ① Gate.io API Key」里填写，
# 程序会保存到 data/credentials.json（该文件已被 .gitignore 忽略）。
#
# 申请地址: https://www.gate.com/myaccount/apiv4keys
# 权限只勾选「合约交易(Futures Trade)」和「读取(Read)」，不要勾「提现」，并绑定IP白名单。
