# Gate.io 多资产 Trend + Carry + 波动率目标 量化策略机器人（图形化版）

基于官方 [gateapi-python](https://github.com/gateio/gateapi-python) SDK 的 USDT 本位永续合约量化交易程序。
**全部操作都在网页图形界面完成**，不需要碰终端、不需要手动编辑配置文件——
双击一次启动脚本，浏览器自动打开，之后填 API Key、选模式、调参数、加币种、启停策略、看回测，全部点鼠标/打字完成。

策略是一套"正经的"系统性交易框架，而不是几个 TradingView 指标的拼盘：多资产 **趋势(Trend, 多周期EWMAC动量)**
+ **Carry(资金费率)** 信号，经过波动率归一化、预测缩放(forecast scaling)后合成，再用 **EWMA协方差矩阵**
把组合缩放到**目标年化波动率**、按标的间相关性分配风险敞口，最后加杠杆/单标的/相关分组等硬约束和调仓缓冲区控制换手。
设计思路参考了 Moskowitz/Ooi/Pedersen 的 Time-Series Momentum 研究、Koijen/Moskowitz/Pedersen/Vrugt 的
Carry 研究，以及 Robert Carver 的开源系统性交易框架 [pysystemtrade](https://github.com/robcarver17/pysystemtrade)
的整体分层方法论（不是照抄代码，是自己按同样的设计原则重新实现，并针对 Gate 只有加密货币永续
+ 黄金/白银等少数贵金属永续合约的实际情况做了适配，比如用资金费率代替期货近月/远月价差作为 Carry 信号）。

> 旧版"15分钟多因子打分 + 双向持仓自动对冲"策略代码整体保留在 `src/engine_legacy_hedge.py`，
> **默认不再运行**，新版系统性策略是唯一的默认策略引擎。保留旧代码只是为了随时可以参考或切回，
> 详见文末"切回旧版对冲策略"一节。

**⚠️ 重要风险提示（请务必阅读）**

- 本程序及策略仅为工程实现，**不构成投资建议**，不保证盈利，历史/模拟/回测表现不代表未来收益。
- 合约交易使用杠杆，亏损可能超出预期甚至导致爆仓，加密货币与贵金属合约价格波动剧烈。
- 波动率目标(volatility targeting)会让策略在低波动期自动加杠杆、在高波动期自动减杠杆，
  但这不等于"控制回撤"——市场剧烈波动或流动性骤降时仍可能出现明显亏损。
- 请务必先用 **模拟盘(PAPER)** 跑至少 1-2 周并结合较长窗口的历史回测观察表现，再考虑切换到 **实盘(LIVE)**。
- 申请 API Key 时**务必只勾选"合约交易+读取"权限，不要开启"提现"权限**，并绑定 IP 白名单。
- API Key/Secret 保存在本地 `data/credentials.json` 文件里（明文），不要把 `data/` 目录分享给他人或上传到任何代码仓库。
- 任何自动化交易程序都可能因网络中断、交易所故障、极端行情滑点等原因产生非预期损失，运行前请做好资金规模控制。

## 快速开始（全程零命令行）

**macOS**：双击 `启动.command`
**Windows**：双击 `启动.bat`

第一次运行会自动创建 Python 虚拟环境并安装依赖（1-2分钟），完成后自动打开浏览器网页控制台。
之后每次双击都是秒开。

**如果电脑没装 Python**，启动脚本会检测到并给你三个选择：自动下载安装 Python 3.12（Windows 上
只装给当前用户，不需要管理员权限）、打开官网自己装、或退出。macOS 上如果检测到 Homebrew 会
问你要不要 `brew install python@3.12`。

几个已经处理好的常见坑：

- **Windows 未装 Python 时输入 `python` 会弹出 Microsoft Store**（系统的应用执行别名），
  脚本会识别并跳过这个假程序，优先用 `py` 启动器。
- **依赖装到一半失败**（断网/公司代理）时，`venv` 目录已经建好了。脚本用 `venv/.deps_ok`
  标记文件判断是否真的装完，没装完下次会自动重试，而不是永远跳过安装、卡在 ImportError。
- **装依赖失败会明确提示原因**，并给出清华镜像源的重装命令，而不是甩一串 traceback。
- Windows 批处理已设为 UTF-8 代码页 + CRLF 行尾，中文提示不会乱码。

打开网页后：

1. **设置页 → ① Gate.io API Key**：粘贴你的 API Key/Secret，点"保存"，再点"测试连接"确认能用。
   （申请地址：<https://www.gate.com/myaccount/apiv4keys>，只勾选"合约交易+读取"）
2. **设置页 → ② 运行模式**：先选"模拟盘 PAPER"。
3. **设置页 → ③ 交易标的**：默认已经有 BTC_USDT / ETH_USDT（可按需添加 XAU_USDT 黄金等），
   输入任意 Gate.io 合约代码继续添加或点 × 删除。
4. **设置页 → ④ 策略核心参数**：先用默认值即可（趋势/Carry权重 0.7/0.3、目标年化波动率15%、
   总杠杆上限3倍等），想理解每个参数含义看下文"策略核心参数说明"。
5. 先去 **回测页** 用历史数据跑一遍，确认策略在你选的标的池和参数下表现符合预期。
6. 切回 **仪表盘页**，点"▶ 启动引擎"，就开始跑了。组合概览、持仓、每标的信号、运行日志都实时刷新在这个页面。

想切实盘：先在模拟盘观察一段时间、并用回测检验过参数，确认满意后回到设置页把"运行模式"切成"实盘 LIVE"
（会弹窗二次确认），然后回到仪表盘重新点一次"启动引擎"（模式切换后需要重启引擎才生效）。

## 策略逻辑概述

### 1. 趋势信号：多周期 EWMAC（不是单一均线交叉）

对每个标的分别在 **1H（短趋势）** 和 **4H（主趋势）** 两个周期上，各自计算 4 组不同快慢速度的
指数加权移动均线交叉(EWMAC)子预测：`(8,32)`、`(16,64)`、`(32,128)`、`(64,256)`（数字单位=该周期的K线根数），
每组做波动率归一化：

```
Trend_{f,s} = (EMA_fast(P) - EMA_slow(P)) / sigma_P
```

`sigma_P` 是用 EWMA(λ=0.94) 估计的"每根K线尺度"价格波动，不是年化波动率，确保分子分母量纲一致。
4 组子预测按 Forecast Diversification Multiplier(FDM) 加权合并成该周期的趋势预测，再和另一个周期的
预测按 `short_trend_weight`/`main_trend_weight`（默认各 0.5）合成最终趋势预测。

### 2. 大周期 Regime 过滤（1D）

用日线 EMA(21/55/200) + ADX(14) 把市场状态分类为"趋势向上/趋势向下/震荡"三类：
如果趋势预测方向和大周期 Regime 方向相反，衰减到 `regime_oppose_dampen`（默认0.3）；
如果 Regime 是"震荡"，整体衰减到 `regime_range_dampen`（默认0.6）——避免在明显逆势或无趋势的行情里满仓押注。

### 3. Carry 信号：资金费率

传统期货的 Carry 用近月/远月合约价差，永续合约没有到期日、没有这个价差，改用资金费率近似：

```
年化Carry(做多视角) = -年化资金费率(annualized funding rate)
```

资金费率为负（空头付多头）时对多头是正向 Carry，反之亦然，再除以年化波动率、做预测缩放，得到 Carry 预测。
**已知局限**：回测阶段 Gate 没有历史资金费率查询接口，只能用下载数据那一刻的实时资金费率在整个回测窗口
里近似（README 末尾"局限与后续可做的事"里也会再提一次），实盘/模拟盘运行时则每次都读取当时的实时资金费率，不受此限制。

### 4. 合成预测 + 预测缩放/封顶

```
F = trend_weight * F_trend + carry_weight * F_carry   (默认 0.7 / 0.3)
```

每一层信号在合成前都做过"预测缩放"（滚动窗口内 `E(|F|) ≈ 10`）和封顶（`|F| ≤ 20`），缩放窗口严格只用
截至当前时刻的历史数据（causal，不看未来），这样不同标的、不同信号来源的预测强度才有可比性。

### 5. 组合层：波动率目标 + 相关性感知风险分配

不是"每个标的固定名义金额"或"平均分配"，而是按预测强度和波动率反比分配风险：

```
raw_weight_i = target_vol * (F_i / 10) / sigma_i
```

再用 EWMA 协方差矩阵 Σ（默认按4H或1D K线计算，`covariance_interval` 可调）算组合层面的实际波动率
`sigma_p = sqrt(w'Σw)`，缩放系数 `k = target_annual_vol / sigma_p`，把整组仓位等比缩放到目标波动率，
相关性越高的标的（比如BTC和ETH）天然会因为协方差矩阵的作用被分配到更小的独立权重（组合层面已经有分散化收益）。

然后依次应用硬约束（超出则等比或分组缩小）：单标的敞口上限 `max_instrument_exposure_pct`、
同相关分组敞口上限 `max_correlated_group_exposure_pct`、组合总杠杆上限 `max_leverage`。

### 6. 调仓缓冲区（降低换手/手续费磨损）

只有当"目标仓位"和"当前仓位"的差值超过 `no_trade_buffer_pct`（按满预测F=10时对应的仓位规模折算）才会真正调仓，
避免预测值小幅波动就频繁开平仓被手续费和滑点"磨损"掉利润。

## 执行/信号节奏（与你确认过的多周期方案一致）

| 层级 | 周期 | 说明 |
|---|---|---|
| 主循环复核 / 下单检查 | 15分钟 | 检查调仓缓冲区+成本过滤，决定是否真正下单 |
| 短趋势 EWMAC | 1H | |
| 主趋势 EWMAC | 4H | |
| 大周期 Regime 过滤 | 1D | |
| 波动率估计 | 1H + 4H（EWMA λ=0.94） | |
| Carry(资金费率) | 按各标的实际funding结算周期 | |
| 组合协方差矩阵 | 4H 或 1D（可配置） | |

## 回测（网页"回测"页）——组合级，不是逐标的分开测

在正式跑模拟盘/实盘之前（或者实盘正在运行的同时），可以在网页"回测"页用真实历史K线检验策略表现：

1. 勾选想测的标的（默认=当前配置的全部交易标的，可取消勾选，也可以在"额外标的"里临时加测没配置过的标的）、
   回测天数（预设按钮：30/90/180/365天，或直接改数字），初始资金留空则用当前"策略核心参数"里的值，点"▶ 开始回测"。
2. 任务在后台线程运行，不会卡住网页，也不会影响正在运行的实盘/模拟盘引擎——**两者完全独立，互不干扰**，
   可以放心地一边跑实盘一边跑回测。下方"回测任务"列表会显示进度（下载数据 → 回测计算 → 完成）。
3. 这是**组合级**回测：一次任务对你选的整组标的**同时**跑，直接复用生产环境的
   `vol.py`/`trend.py`/`carry.py`/`portfolio.py`/`backtest.py` 判定逻辑（和实盘/模拟盘完全一致，不是另外写的简化近似版），
   输出的是**组合层面**的权益曲线和统计指标，而不是把每个标的分开单独测。
4. 点任意一条任务可以查看完整结果：CAGR(年化收益率)、年化波动率、Sharpe、Sortino、最大回撤、Calmar、
   年化换手率、交易数、权益曲线图、分标的贡献（每个标的的净盈亏/手续费/资金费/最新预测值）、逐笔交易明细。
   任务历史会保存在 `data/backtests/`，重启程序后依然能看到。

**关于回测的几点说明（务必了解，避免误读结果）：**

- 组合协方差矩阵在回测里用 O(n) 的一次遍历预先算出"逐时间点快照序列"，再用因果(asof)查找定位到每个决策
  时刻对应的快照——这是性能优化，不影响正确性（只用过去数据，不看未来）。
- 主时间轴按"短趋势周期"(1H)walk，而不是逐15分钟走查——因为信号管线里没有任何东西比1H变化更快，
  这样做和逐15分钟走查在行为上完全等价，但快得多，验证过3标的×150天数据能在3秒内跑完整个回测。
- Carry 信号用的是**下载数据那一刻的单一资金费率快照**在整个回测窗口里近似，不是逐根K线的真实历史资金费率
  （Gate 没有历史资金费率查询接口），这是当前版本的已知局限，见下文"局限与后续可做的事"。
- 历史K线会按"合约代码+周期"缓存在 `data/klines/`，同一标的重复回测只需要补最新一小段数据，速度会快很多；
  如果怀疑缓存数据有问题，直接删除 `data/klines/` 目录里对应的 CSV 文件即可强制重新下载。
- Gate 交易所K线历史接口本身有约10000根的深度上限，程序会自动按周期换算成天数上限并提前裁剪你填的回测天数，
  超限会在回测结果的"警告"区提示，不会导致任务直接失败。
- 和所有回测一样，**历史表现不代表未来收益**，交易笔数太少时统计结果参考意义有限，建议结合较长的回测窗口
  和后续模拟盘的实盘运行数据一起综合判断；也建议在"更换标的池/大幅调整核心参数"后都重新跑一遍回测。

## 策略核心参数说明（网页"设置"页可直接改）

| 参数 | 含义 |
|---|---|
| 模拟盘/回测初始资金 | 仅 paper模式/回测 默认初始资金，live模式直接用账户真实权益 |
| 趋势/Carry信号权重 | 最终合成预测 F 里两类信号的权重，默认 0.8/0.2（趋势为主导，carry 权重调低是为了在强趋势期间不被稀释） |
| 短趋势(1H)/主趋势(4H)权重 | 趋势预测内部两个周期的权重，默认 0.6/0.4（1H权重调高，让策略更快响应新趋势的启动） |
| 组合目标年化波动率 | 整个组合缩放的目标，越高整体仓位越激进 |
| 组合总杠杆上限 | 硬约束，防止极端行情下杠杆失控，默认4倍 |
| 单标的敞口上限 | 防止单一标的过度集中，默认55% |
| 相关分组敞口上限 | 防止"表面上分散实际高度相关"的标的（如BTC/ETH）过度集中 |
| 调仓缓冲区 | 越大换手越少，越省手续费，但对信号变化的响应也越慢；只对"加仓/开仓"生效(见下一行)，默认6% |
| 减仓/平仓缓冲区倍数 | 减仓/平到0 实际用的缓冲区 = 调仓缓冲区 × 这个倍数，默认4倍，让仓位对forecast短暂走弱更迟钝，避免趋势没走完就被削减；加仓/开仓不受影响，仍然用原始调仓缓冲区，对趋势走强保持灵敏 |
| 反手确认：最短持仓小时数 | 方向反转(多变空/空变多)前至少要维持当前仓位这么多小时，抑制forecast噪声导致的频繁反手，默认8小时 |

## 调参笔记：减少"漏趋势"（2024版默认参数已按此调整）

如果观察到策略经常"该有仓位的时候没仓位、或者仓位小得不成比例"，根源通常不是核心算法本身，而是几个参数在联合压制信号：

- **Regime过滤滞后**：大周期Regime是按日线算的，天然比1H/4H趋势信号慢一拍，新趋势刚启动时Regime可能
  还没转向甚至还在原方向的反面——`regime_oppose_dampen`/`regime_range_dampen`如果压得太狠(旧默认0.3/0.6)，
  会正好把趋势最早、往往也最赚钱的一段砍掉大半仓位。已调松到0.5/0.8，`regime_adx_trend_threshold`
  也从20降到15，让更多真实趋势更早被认成"趋势市"而不是"震荡市"。
- **预测缩放窗口过长**：`forecast_scale_lookback`如果设得很大（旧默认2500根），forecast的"标定基准"
  会用到几个月前、可能完全不同波动率环境下的数据，压低当前这一波趋势的forecast强度。已缩短到1500。
- **开仓缓冲区偏宽**：`no_trade_buffer_pct`如果偏大（旧默认10%），新趋势的forecast要长到足够大才能
  突破缓冲线开仓，等于给入场加了一道门槛。已收窄到6%——**这个改动只影响开仓/加仓的灵敏度，不影响
  减仓/平仓**（减仓/平仓已经用`exit_buffer_multiplier`单独放宽过，两者互不干扰）。
- **敞口/杠杆约束偏紧**：即使信号本身判断对了方向，`max_leverage`/`max_instrument_exposure_pct`如果
  设得太保守（旧默认3倍/40%），真正强趋势也拿不到应有的仓位规模。已适度放宽到4倍/55%。

⚠️ 以上调整里，**放宽杠杆/敞口上限、收窄开仓缓冲区**会直接提高换手率和最大回撤/爆仓风险，属于
"用更高的风险换取更完整地捕捉趋势"的取舍，不是无代价的。已经用一段"震荡-强趋势-震荡-强趋势-震荡"
交替的合成行情做过回测对比：新参数相对旧参数总收益率从25.6%提升到30.9%（净收益已扣除手续费，
换手率也从60倍/年升到124倍/年），说明净效果是正的，但这是特定合成数据下的结果，不代表在你实际
交易的标的池上一定有相近幅度的提升，务必自己重新跑一遍回测、并观察一段时间模拟盘表现再决定是否
上实盘，不满意可以随时在设置页把参数调回更保守的旧值(3倍/40%/10%/0.3/0.6/20/2500)。

"高级参数"折叠面板里是 Regime 过滤细节、EWMA/FDM/协方差相关的内部参数、执行节奏（各周期用哪个K线粒度），
一般不需要改，改之前建议理解清楚每个参数的作用（可参考上面"策略逻辑概述"）。

EWMAC 的4组(快,慢)周期对(默认`(8,32)`/`(16,64)`/`(32,128)`/`(64,256)`)暂不支持网页表单直接改，
如需调整请编辑 `config.yaml` 或直接改 `src/config.py` 里 `SystematicConfig.ewmac_horizons`。

## 反手确认（抑制频繁反手/过高换手）

如果发现回测/实盘里同一个标的短时间内反复"多转空、空转多"（每隔几小时就反手一次，每次都被手续费+滑点小幅侵蚀），
根源是 no-trade buffer 只能挡住"小幅调仓"，挡不住"方向反转"——多变空时目标仓位和当前仓位的差值天然很大，
几乎总会超过buffer、总会触发反手，forecast一过零轴（噪声很容易做到这点）就会整仓反手一次。

"策略核心参数"里的**反手确认：最短持仓小时数**就是为此设计的：方向反转前，当前仓位必须已经持有满这么多
小时，否则本轮维持现状（既不平仓也不反手），直到满足最短持仓时间、且信号仍然要求反向，才会真正执行反手。
同方向加/减仓、正常的"平到0"退出都不受影响。回测结果里的"反手确认拦截次数"就是这个机制生效的次数，
数值越大说明帮你挡掉的噪声反手越多。想更激进地抑制换手可以调大这个值，但太大会让策略对真实趋势反转的
响应变慢，建议结合"K线图"页面对着实际标的走势观察调整。

## ⚠️ 反向执行（人工干预开关）

"设置页 → ⑤ 反向执行"是一个实验性的人工干预开关，默认关闭。开启后，预测计算、波动率归一化、组合风险分配、
杠杆/敞口约束、反手确认防抖——**全部照常按原来的逻辑计算**，只在最终真正下单这一步，把方向对调：算出来该
开多，实际执行开空；算出来该开空，实际执行开多。平仓方向由持仓方向决定，也会自动跟着倒过来。

这不是"模型认为反过来更对"，纯粹是给你一个人工干预的手段——如果你观察到某个标的池/参数组合下，策略的
判断持续和实际走势相反（这种情况理论上不该系统性出现，但不排除某些市场阶段/参数组合下确实如此），可以用
这个开关快速验证"反过来做"是不是真的更好，而不用重写一遍信号逻辑。

**务必先在"回测"页用相同的标的池和参数验证过"反向执行"确实表现更好，再考虑在模拟盘/实盘打开它**，否则很
可能只是把亏损方向从"做多"换成"做空"而已，本质没有解决问题。开启后仪表盘标题栏和"K线图"页面都会有醒目的
⚠️ 提示，运行日志里每次调仓也会同时记录"计算方向"和"实际执行方向"，避免你忘记这个开关是开着的。

## 滚动样本外验证 与 趋势捕获诊断

回测页勾选"同时做滚动样本外验证"后，除了常规结果还会多出两块内容。

**趋势捕获诊断**（每次回测都会自动做）自动识别行情里幅度≥8%的显著趋势段，逐段打分，
用来分辨"到底是漏趋势，还是抓到了但拿不满"：

| 指标 | 含义 | 怎么读 |
|---|---|---|
| 参与率 | 有多少趋势段我们有仓位 | 低 = 真的漏了趋势 |
| 捕获率 | 行情动了X%，账户赚了Y%，Y/X | 参与率高但这个低 = 方向对但仓位太小 |
| 入场滞后 | 趋势走了百分之多少才进场 | 高 = 入场太慢 |
| 离场过早 | 趋势还剩百分之多少就走了 | 高 = 拿不住 |

**一个反直觉但重要的发现**：在合成的多段趋势行情上跑这个诊断，结果是参与率100%、
入场滞后中位数只有5-6%、离场过早0%，但捕获率中位数只有26-33%。也就是说策略方向对、
进场早、也拿得住，**真正的瓶颈是仓位规模而不是择时**。原因是波动率目标：单标的权重
≈ 目标年化波动率 ÷ 该标的年化波动率，15%的目标除以加密资产60%左右的波动率，本来就
只有约25%的仓位——捕获率大约就等于这个比值。所以想吃到更多，主要的杠杆是
`target_annual_vol_pct`，而不是继续改入场/出场逻辑。

⚠️ 但上面这个结论是在**合成数据**上得到的，合成行情的趋势比真实市场干净得多。
请务必在你自己真实的标的池和历史上重新跑一次诊断再下判断——这也正是把它做成常驻工具
而不是一次性分析的原因。

**滚动样本外验证**把窗口切成若干折分别统计。看的时候**不要只看均值**，要看各折之间的
离散程度、正收益折数占比、最差一折有多差。如果只有一两折特别好、其余平平，说明整体
收益是被某段特殊行情撑起来的。

需要说明清楚的是：本策略的参数是人工设定的、不是从数据里拟合出来的，所以这里做的是
**跨时间稳定性检验**，不等于"已经做过防过拟合验证"。以后如果真的引入需要训练的模型
（比如概率型反转分类器），必须在这个框架上再加 purged & embargoed 的交叉验证，
否则三重屏障标签的窗口重叠会造成训练集/验证集泄漏，回测会好看得离谱而实盘失效。

`src/walkforward.py` 里还有 `parameter_sweep()`，可以扫描单个参数看收益/Sharpe曲面。
判读原则：曲面应该是平滑的小山包，而不是某个取值上的孤立尖峰——尖峰几乎必然是拟合了
历史噪声。目前对 `target_annual_vol_pct` 扫描出来的曲面是平滑单调的，属于健康形态。

## 交易标的与黄金

网页"设置 → 交易标的"里可以自由输入任意 Gate.io **USDT 本位永续合约**代码，不做限制。
黄金对应合约代码为 `XAU_USDT`（Gate 贵金属永续合约区，白银为 `XAG_USDT`）。
Gate 的贵金属合约本质仍是"永续合约"，通过资金费率机制模拟24小时连续交易，
和外汇/CFD 平台按自然日收取隔夜利息(overnight swap)的机制不同，但效果类似，程序统一按资金费率处理。
具体合约代码是否仍然上线交易，请以 Gate 官网合约列表为准。

策略引擎全程只在**同一方向**持有单一仓位（不会同一标的同时开多又开空），账户仍运行在 Gate 的
"双向持仓模式(dual mode)"下（引擎启动时会自动尝试切换，切换前提是该结算币种下没有任何持仓/挂单），
只是这只是账户层面的模式设置，新策略本身不使用"同时对冲两条腿"的逻辑（那是旧版策略的做法）。

## 版本与更新

程序会在启动时后台静默检查一次更新（失败不影响启动），也可以随时在网页
「⬆️ 检查更新」页手动检查。检测到新版会显示更新内容，可以选择立即更新、
跳过这个版本、或去 GitHub 查看。

git clone 安装的用户可以一键 `git pull` 更新——你的 `data/`（API Key、
数据库、K线缓存）和 `config.yaml` 都被 gitignore 忽略，**不会被更新覆盖**。
下载 zip 安装的用户需要手动把这两个复制到新目录。

更新前会强制检查：引擎必须已停止、且没有未平仓持仓——更新会替换正在执行的
策略代码，而仓位是真金白银。

如果你是这个项目的维护者，发布新版本的完整流程见 [RELEASING.md](RELEASING.md)，
一行命令：`./scripts/release.sh 1.0.0`。

## 目录结构

```
gate_quant_bot/
├── 启动.command / 启动.bat   # 双击启动（macOS / Windows）
├── main.py                    # 纯图形化启动入口，无需任何命令行参数
├── config.example.yaml        # 配置文件模板（首次运行会自动复制为 config.yaml）
├── requirements.txt
├── src/
│   ├── config.py               # 配置加载+热重载+网页表单写回，SystematicConfig等
│   ├── credentials.py          # API Key本地存储（网页表单填写，替代手动编辑.env）
│   ├── models.py                # Position/Trade/Signal 数据结构
│   ├── vol.py                   # EWMA波动率/协方差估计（新策略地基模块）
│   ├── trend.py                  # 多周期EWMAC趋势信号 + Regime过滤
│   ├── carry.py                  # 资金费率Carry信号
│   ├── portfolio.py              # 组合层波动率目标 + 相关性感知风险分配
│   ├── costs.py                  # 手续费/资金费/滑点成本模型
│   ├── exchange_gate.py          # Gate.io 实盘接口封装（双向持仓 dual mode）
│   ├── exchange_paper.py         # 模拟盘（真实行情+虚拟成交）
│   ├── state.py                  # 线程安全状态 + SQLite持久化
│   ├── engine.py                 # EngineController(供网页启停引擎)
│   ├── systematic_engine.py      # 新版策略主循环：多资产Trend+Carry+波动率目标
│   ├── engine_legacy_hedge.py    # [默认不使用] 旧版15分钟多因子+双向对冲策略实现，保留供参考
│   ├── data_fetcher.py           # 历史K线分页下载 + 本地CSV缓存（仅回测使用，只读行情）
│   ├── backtest.py               # 组合级回测引擎：逐根K线走查，复用生产策略/风控逻辑
│   ├── backtest_runner.py        # 回测任务控制器：后台线程管理，和实盘引擎完全独立
│   └── dashboard_web.py          # 全图形化网页控制台（唯一操作入口，含"回测"页）
└── data/                        # 运行后自动生成：SQLite数据库、日志、API Key存储、K线缓存、回测结果
```

> `src/dashboard_terminal.py` 是更早版本留下的终端仪表盘代码，**当前版本不再使用它**，保留只是为了不破坏历史文件，可以忽略或删除。

## 切回旧版对冲策略

如果想切回"15分钟多因子打分 + 双向持仓自动对冲"的旧策略（`src/engine_legacy_hedge.py` 里的 `Engine` 类），
把 `src/engine.py` 里 `EngineController.start()` 中构造 `SystematicEngine(...)` 的地方换成
`Engine(...)`（构造参数和旧版一致，配置文件里 `risk`/`strategy`/`hedge` 三段参数原样保留可直接使用），即可切回。
两套策略共用同一个 `StateStore`/`GateExchange`/`PaperExchange`，但不建议同时跑（引擎同一时刻只有一个在运行）。

## 数据持久化与崩溃恢复

所有已平仓交易和权益曲线都会实时写入 `data/state.sqlite3`，程序重启后会自动读回历史统计
（总盈亏、胜率、平均持仓时间等）。持仓中的仓位状态以交易所返回的真实持仓为准（`_reconcile_positions`
每个tick都会核对），如果程序异常退出后重启，会自动纳入管理，不会因为本地状态丢失而失去对持仓的掌控。

## 局限与后续可做的事（当前版本范围）

当前版本交付的是策略框架的核心层（信号生成→组合风险分配→执行→组合级回测），以下是你原始需求里
提到、但为了先把核心跑通而**有意简化或推迟**的部分，如果需要可以后续再加：

- **Carry信号的历史资金费率近似**：回测阶段用单一实时快照近似整个窗口，不是逐根K线的真实历史值。
- **Forecast Diversification Multiplier / 协方差**：当前是简化版实现（滚动相关性/EWMA协方差），
  没有做 Ledoit-Wolf 收缩估计等更稳健的协方差估计方法。
- **压力测试套件**：参数扰动测试（如 EWMAC 14/56 到 18/72 逐步扫描看 Sharpe 曲面是否平滑）、
  成本翻倍测试、波动率冲击测试——当前没有内置，可以手动改参数多跑几次回测对比，但没有自动化的扫描工具。
- **Walk-Forward 滚动样本外验证**：当前回测是对选定窗口整体跑一遍（因果、不看未来，但没有做
  "样本内定参数、样本外测试、滚动前进"的完整walk-forward流程）。
- **蒙特卡洛/Bootstrap 检验**：没有做收益率bootstrap重采样、P(最大回撤>30%)、P(收益<0)等统计量。
- **动态信号权重**：趋势/Carry权重目前是固定的0.7/0.3，没有做样本内优化或自适应调整。

这些都是提升"这套系统的严谨性/稳健性证据"的方向，而不是当前策略无法运行的缺陷——现在这套框架
已经是完整可跑、逻辑自洽、经过分层数值验证的系统，只是研究深度还有继续加深的空间。

## 常见问题

- **为什么模拟盘也需要真实 API Key？** 模拟盘只用 API Key 读取公开行情（K线、价格、资金费率、合约参数），
  不会用来下单或访问账户资金，用真实数据模拟是为了让表现尽量贴近真实市场磨损。
- **切换双向持仓模式失败怎么办？** 最常见原因是账户里还有旧的持仓或挂单，去 Gate 官网合约页面手动平仓/撤单后，
  回到网页点"停止引擎"再"启动引擎"重试。
- **改了设置页的参数没生效？** 策略核心/Regime/高级参数是热更新的，等下一次tick（默认15分钟内）就会用上新值；
  但"运行模式"和"API Key"的修改需要手动点一次"停止引擎"+"启动引擎"（因为要重新建立交易所连接）。
- **为什么某个标的一直没有仓位/信号？** 可能是该标的对应周期的历史K线数不足 `min_bars_short`/`min_bars_main`/
  `min_bars_regime`（数据不够可靠时会跳过该标的），也可能是波动率目标+相关分组约束把它的权重压得很小，
  属于正常的风险分配结果。
