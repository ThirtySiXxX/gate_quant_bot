# 怎么发布新版本

## 一句话版本

```bash
# 1. 在 CHANGELOG.md 顶部加一节写清楚改了什么
# 2. 跑发布脚本
./scripts/release.sh 1.0.0
```

脚本会自动做完检查、写版本号、提交、打标签、推送。用户端下次打开程序就能看到更新提示。

---

## 用户是怎么收到更新的

程序里的检查逻辑（`src/updater.py`）按这个顺序找版本：

1. **先看 GitHub Releases** — 有正式 Release 时用它（`tag_name` 当版本号，Release 正文当更新说明）
2. **没有 Release 就直接读仓库里的 `VERSION` 文件** — 更新说明从 `CHANGELOG.md` 里抽对应章节

**所以最省事的发布方式就是：改 `VERSION` + 写 `CHANGELOG.md` + push。** 不建 Release 也能正常推送更新。
建 Release 的额外好处只是给非 git 用户一个现成的 zip 下载链接。

用户那边：
- **git clone 装的** → 点「立即更新」直接 `git pull`，`data/` 和 `config.yaml` 因为被 gitignore 忽略，不会被覆盖
- **下载 zip 装的** → 只给下载链接，需要手动把 `data/` 和 `config.yaml` 复制到新目录

---

## 标准流程

### 第 1 步：写 CHANGELOG

在 `CHANGELOG.md` 的 `---` 分隔线下面、最新版本上面，插入新的一节：

```markdown
## 1.0.0 — 2026-09-15

### 🔒 资金安全相关
- ⚠️ **改了什么。** 为什么改，对用户有什么影响。

### 新增功能
- ...

### 修复
- ...
```

**格式要求（脚本会强制检查）：**
- 标题必须以 `## ` 开头且**包含版本号** —— 程序就是靠这个定位章节的
- 这一节至少要有 2 行实际内容

**写法建议**：每条写清楚「改了什么 + 为什么 + 对用户有什么影响」。用户在「检查更新」页看到的就是这段文字，写"修复若干问题"等于什么都没说。会改变实盘行为的用 ⚠️ 标注，和资金安全相关的用 🔒。

### 第 2 步：跑发布脚本

```bash
./scripts/release.sh 1.0.0
```

脚本依次做 6 件事，任何一步不过就中止，不会留下半成品：

| 步骤 | 检查内容 |
|---|---|
| 1 | 版本号格式对不对；是否真的比当前版本新（用 `sort -V` 做语义化比较） |
| 2 | CHANGELOG 里有没有这个版本的章节，且内容不为空 |
| 3 | 工作区干不干净、标签是否已存在、本地是否落后远端；顺便清掉残留的 `.lock` |
| 4 | 跑单元测试 + 全量语法检查 |
| 5 | 写 `VERSION`、提交、打 `v1.0.0` 标签 |
| 6 | 询问确认后推送分支和标签 |

推送成功后会打印建 GitHub Release 的链接，想建就点进去，标题填 `v1.0.0`，正文把 CHANGELOG 那节粘进去。

### 第 3 步（可选）：建 GitHub Release

只有在你希望非 git 用户能下载 zip 时才需要。打开脚本给出的链接，或者：

```
https://github.com/<你的用户名>/<仓库名>/releases/new?tag=v1.0.0
```

---

## 版本号怎么定

`主版本.次版本.修订号`，程序按数值比较（所以 `1.10.0` 确实大于 `1.9.0`）。

| 情况 | 该动哪位 | 例子 |
|---|---|---|
| 只修 bug，不改行为 | 修订号 | `0.9.0` → `0.9.1` |
| 加功能，或改了策略默认参数 | 次版本 | `0.9.1` → `0.10.0` |
| 配置不兼容、需要用户手动迁移 | 主版本 | `0.10.0` → `1.0.0` |

这个项目有个特殊考虑：**改动策略默认参数（比如缓冲区、目标波动率）会直接改变实盘行为**，即使代码只动了一个数字，也应该升次版本号并在 CHANGELOG 用 ⚠️ 标出来，别当成修订号悄悄发出去。

---

## 发布前的自查

跑脚本之前顺手确认一下：

```bash
# 密钥没被跟踪（应该没有任何输出）
git status --porcelain | grep -i credential

# 确认被忽略（应输出 .gitignore 里的规则）
git check-ignore -v data/credentials.json
```

如果这个项目要给别人用，还要确认：

- 仓库是**公开**的 —— 匿名 GitHub 接口读不到私有仓库，更新检查会失败
- `requirements.txt` 里加了新依赖的话，在 CHANGELOG 里提一句，让用户知道要重启程序（启动脚本会自动补装）

---

## 常见问题

**用户说检查不到更新？**

1. 确认 `VERSION` 真的推上去了：`git show origin/main:VERSION`
2. 确认仓库是公开的
3. GitHub raw 内容有几分钟 CDN 缓存，刚推完可能要等一会

**推错了想撤回？**

```bash
git tag -d v1.0.0
git push origin :v1.0.0        # 删远端标签
git revert HEAD                # 回滚版本号提交
```

注意**不要用 `git push -f` 改写已推送的历史** —— 用户那边 `git pull` 会因为历史分叉而失败。

**用户 `git pull` 报冲突？**

通常是他们改过本地代码。程序里的自动更新在检测到未提交改动时会主动中止，不会覆盖他们的修改，会提示先 stash。

**发完发现有严重 bug？**

直接发下一个修订号（`1.0.1`），比撤回干净。在 CHANGELOG 里写明这是修 `1.0.0` 的什么问题。
