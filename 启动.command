#!/bin/bash
# 双击这个文件即可启动程序（macOS）。
# 首次运行会自动创建虚拟环境并安装依赖，可能需要1-2分钟，请耐心等待；
# 之后每次双击都会秒开，并自动打开浏览器网页控制台。

cd "$(dirname "$0")"

if [ ! -d "venv" ]; then
  echo "============================================================"
  echo " 首次运行，正在创建运行环境并安装依赖，请稍候（1-2分钟）..."
  echo "============================================================"
  python3 -m venv venv
  source venv/bin/activate
  pip install --upgrade pip >/dev/null 2>&1
  pip install -r requirements.txt
else
  source venv/bin/activate
fi

echo ""
echo "============================================================"
echo " 正在启动量化策略控制台，浏览器会自动打开..."
echo " 如需停止程序，直接关闭这个终端窗口即可。"
echo "============================================================"
echo ""

python3 main.py

read -p "程序已退出，按回车关闭此窗口..."
